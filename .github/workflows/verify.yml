name: verify
on:
  push:
    branches:
      - master
  pull_request:
  workflow_dispatch:

jobs:
  test-linux-cpu:
    runs-on: "linux.2xlarge"
    steps:
      - name: check os
        run: cat /etc/os-release
